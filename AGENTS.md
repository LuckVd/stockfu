# AI 工作区约定

## 每次冷启动

1. 先读 `.local/WORKSTATE.md`；文件不存在时，以 `docs/WORKSTATE_TEMPLATE.md` 创建它。
2. 检查 `pwd`、`git branch --show-current` 与 `git status --short`，确认当前 worktree、分支和未提交改动。
3. 简短说明当前工作方向与本次准备执行的动作；任务与状态文件方向冲突时，先请用户确认是否切换 worktree。

## 交接状态

- 在完成一个明确阶段、切换工作目标、启动/结束后台任务或结束会话前，更新 `.local/WORKSTATE.md`。
- 只保留当前目标、已验证结论、未提交改动、下一步、阻塞项及仍有效的运行任务；目标长度为一屏（约 300–600 字）。
- 不写完整对话、长日志、可从代码或正式文档获取的背景。长期有效的设计决定应写入 `docs/`。
- 已完成事项、失效 PID/日志与过期计划应及时移除；若状态超过 7 天未更新，先标记为可能过期并核验。

## Worktree

- 当前目录就是唯一允许修改的 worktree；不要切换、重置或修改其他 worktree，除非用户明确要求。
- 后台任务与 SQLite 写操作应标注在状态文件中。不同 worktree 并行运行时，使用不同数据库路径，避免共享数据库写锁。
