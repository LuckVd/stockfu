# AI 工作区约定

## 每次冷启动

1. 先读 `.local/WORKSTATE.md`；文件不存在时，以 `docs/WORKSTATE_TEMPLATE.md` 创建它。
2. 检查 `pwd`、`git branch --show-current` 与 `git status --short`，确认当前分支和未提交改动。
3. 简短说明当前工作方向与本次准备执行的动作；任务与状态文件方向冲突时，先请用户确认。

## 交接状态

- 在完成一个明确阶段、切换工作目标、启动/结束后台任务或结束会话前，更新 `.local/WORKSTATE.md`。
- 只保留当前目标、已验证结论、未提交改动、下一步、阻塞项及仍有效的运行任务；目标长度为一屏（约 300–600 字）。
- 不写完整对话、长日志、可从代码或正式文档获取的背景。长期有效的设计决定应写入 `docs/`。
- 已完成事项、失效 PID/日志与过期计划应及时移除；若状态超过 7 天未更新，先标记为可能过期并核验。

## Git / 分支

- **每个任务开一个普通分支**（不用 worktree）：在分支上提交，完成后开 PR 合并到 `main`。
- **不直接提交 `main`**，除非用户明确要求「直接提交」。
- 分支名简短达意（如 `docs/...`、`fix/...`、`feat/...`）。
- 后台任务与 SQLite 写操作应标注在状态文件中；多个任务并行时使用不同数据库路径，避免共享数据库写锁。

## 宿主后台任务核验

- 沙箱内的 `ps` / `pgrep` 只能看到隔离命名空间，**不得**据此判断宿主机或工作区外启动的任务已经退出。
- 查询后台任务状态时，默认在宿主全局进程表中只读核验：进程命令行、已知 PID、运行时长、日志更新时间与必要的 checkpoint / 数据库写入时间；排除查询命令自身后再报告结论。
