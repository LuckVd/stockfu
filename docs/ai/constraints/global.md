# Global Constraints

- Use Chinese for all user-facing natural language output.
- Keep commands, file paths, configuration keys, code, and identifiers in their original form.
- Never auto-commit or auto-push without explicit user confirmation.
- Prefer incremental changes that fit the existing architecture.
- Keep implementation aligned with current project conventions.
- Ask before resolving any mismatch between workflow docs and code.
