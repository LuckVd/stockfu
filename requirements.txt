# ===== StockFu · 资产管理终端 =====
# 本文件是人工维护的「浮动依赖声明」，仅作为更新 requirements.lock 的输入，
# 不得用于部署/测试安装。安装一律使用锁文件（含全部传递依赖、精确版本与 hash）：
#   UV_BREAK_SYSTEM_PACKAGES=1 uv pip install --system -r requirements.lock
#   (国内可用代理或 -i https://pypi.tuna.tsinghua.edu.cn/simple)
# 更新锁：uv pip compile requirements.txt -o requirements.lock --generate-hashes
# 可复现/canonical 运行以 requirements.lock 为准，并按 lock 重建环境。

# Web / API
fastapi
uvicorn[standard]
python-multipart   # 表单/文件上传（CSV 导入）

# ORM / 存储 (SQLite)
sqlmodel
sqlalchemy

# ===== 数据源（免费公开；成交用前复权 qfq；股息率用 close_raw 不复权）=====
akshare        # A股: 行情/分红/ETF(前复权)/板块；港美 qfq
efinance       # 东财: A股日K fqt=1 前复权
yfinance       # 全球: 港美股 auto_adjust 前复权 + dividends
baostock       # 日K adjustflag 1/2/3=后/前/不复权 + PE/PB + 分红
# tushare      # 可选: 配 TUSHARE_TOKEN 后作为高质量A股源（需注册）

# 健壮性 / 网络
tenacity       # 数据层重试
httpx          # 异步 HTTP（新闻/推送）
PySocks        # baostock 裸 TCP 走 Clash SOCKS(三复权回补防黑名单)

# 配置
pydantic
pydantic-settings

# 定时任务（每日抓取/落库/推送）
apscheduler

# 邮件定时发送（无头浏览器把分享卡片渲染成图 → SMTP 发出）
playwright     # 装后需运行 `playwright install chromium` 下载浏览器

# AI 决策报告（OpenAI 兼容协议，httpx 直连；json_repair 容错解析 LLM 的 JSON 输出）
json_repair
# openai       # 可选：若改用官方 SDK
# anthropic
